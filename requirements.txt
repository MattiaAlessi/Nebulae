# NEBULAE Python Dependencies
# Install with: pip install -r requirements.txt

# Tor control
stem>=1.8.2

# SOCKS5 proxy for Tor routing
PySocks>=1.7.1

# Rich terminal UI (CLI mode)
rich>=13.7.0

# SQLCipher Python bindings (optional but recommended)
# pysqlcipher3>=1.2.0  # Requires libsqlcipher-dev

# Async HTTP (for bridge info)
aiohttp>=3.9.0

# Type checking helpers
typing-extensions>=4.9.0

# Maturin for building Rust extension
maturin>=1.5.0

# Tests
pytest>=8.0.0
